server:
  port: 8001 #运行端口
spring:
  application:
    name: eureka-server #服务名称
eureka:
  instance:
    hostname: localhost #主机地址
  client:
    fetch-registry: false #指定是否从注册中心获取服务（注册中心不需要获取）
    register-with-eureka: false #指定是否注册到注册中心（注册中心不需要注册）
